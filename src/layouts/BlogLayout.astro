---
import type { BaseProps, WithComponent } from "@core/types/astro";
import BaseLayout from "@layouts/BaseLayout.astro";

type Props = BaseProps &
	Pick<
		WithComponent<typeof BaseLayout>,
		"locale" | "title" | "description" | "author"
	>;

const { locale, title, description, author } = Astro.props;
---

<BaseLayout
	site="KCBlog"
	{locale}
	{title}
	{description}
	{author}
	favicon="/favicon.ico"
	favicon:light="/favicon-light.svg"
	favicon:dark="/favicon-dark.svg"
>
	<slot />
</BaseLayout>
